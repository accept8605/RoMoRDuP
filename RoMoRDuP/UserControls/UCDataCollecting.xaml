<UserControl x:Class="RoMoRDuP.UserControls.UCDataCollecting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <StackPanel>
            <Label Margin="5" FontWeight="Bold" FontSize="14">Collecting Data</Label>
            <Label>Found Playlists:</Label>
            <TextBox Margin="5" IsReadOnly="True" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" MinWidth="300" MinHeight="200" HorizontalAlignment="Stretch"></TextBox>
            <CheckBox Margin="5">Autoscrolling</CheckBox>
            <Label>Searching playlists</Label>
            <ProgressBar Height="15" Margin="5" IsIndeterminate="True"></ProgressBar>

            <GroupBox Margin="5" Header="Creating file tree">
                <ProgressBar Minimum="0" Maximum="100" Height="15" Margin="5,0,5,5" Value="50"></ProgressBar>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
